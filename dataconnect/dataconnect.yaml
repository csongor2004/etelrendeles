specVersion: "v1"
serviceId: "etelrendeles"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "etelrendeles-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
